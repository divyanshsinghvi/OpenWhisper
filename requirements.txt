# Python dependencies for the streaming engines and helper scripts.
# Install with: pip install -r requirements.txt
# (or use ./install-nemotron.sh which also downloads the ONNX model bundle.)

# Nemotron streaming engine (default)
sherpa-onnx>=1.13          # ONNX streaming recognizer
sounddevice>=0.4.6         # Microphone capture (cross-platform)
soundfile>=0.12.1          # WAV I/O (used by fake-mic test mode)
numpy>=1.24                # Audio buffers
huggingface_hub>=0.20      # Model download in install-nemotron.sh

# Moonshine streaming engine (fallback)
moonshine-voice            # Streaming Moonshine v2 SDK

# Windows-only audio fallback
PyAudioWPatch>=0.2.12; sys_platform == "win32"
